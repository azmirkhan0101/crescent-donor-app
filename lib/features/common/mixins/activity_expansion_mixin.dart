// import 'package:cresent_charge_user_app/features/donation/controllers/round_up_controller.dart';

// /// Mixin to provide activity expansion functionality
// mixin ActivityExpansionMixin {
//   /// Map to track expansion state of activities
//   Map<String, bool> get activityExpansionStates;

//   /// Generate unique key for an activity based on its content and index
//   String getActivityKey(RecentActivity activity, int index) {
//     return '${activity.brandName}_${activity.purchaseAmount}_${activity.timeAgo}_$index';
//   }

//   /// Check if a specific activity is expanded
//   bool isActivityExpanded(String activityKey) {
//     return activityExpansionStates[activityKey] ?? false;
//   }

//   /// Toggle expansion state of a specific activity
//   void toggleActivityExpansion(String activityKey);
// }
